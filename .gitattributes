# Shell scripts must keep LF endings even in a Windows checkout with core.autocrlf=true.
# They are executed inside Linux containers, and Sim.Containerfile copies them in from the
# build context. A CRLF shebang makes exec fail with "No such file or directory" naming the
# script itself, or with a cascade of `$'\r': command not found`.
*.sh text eol=lf
